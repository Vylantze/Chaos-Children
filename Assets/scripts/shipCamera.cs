using UnityEngine;
using System.Collections;

public class shipCamera : MonoBehaviour {
	//private GameObject playerMain;
	// Use this for initialization
	void Awake () {
		//GameObject.Find ("Starship").SetActive (true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
